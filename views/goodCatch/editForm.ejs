<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles.css">
    <title>Update Good Catch</title>
</head>
<body>
    <div class="background-image">
        <div class="form-container">
            <h1>Update Good Catch</h1>
            <form method="POST" action="/goodCatch/update/<%= goodCatch._id %>">
                <label for="site">Site:</label>
                <input type="text" id="site" name="site" value="<%= goodCatch.site %>" required>
                
                <label for="department">Department:</label>
                <input type="text" id="department" name="department" value="<%= goodCatch.department %>" required>
                
                <label for="description">Description:</label>
                <textarea id="description" name="description" required><%= goodCatch.events[0].description %></textarea>
                
                <button type="submit">Update</button>
            </form>
            <form action=/users/<%= user._id %>/catches">
                <button type="submit">Personal Dashboard</button>
            </form>
        </div>
    </div>
</body>
</html>
